# Lightweight FinFET14nm Mock PDK subset

This generated PDK subset is for structure recognition only.  It intentionally
omits layout generator Python modules and keeps `models.sp`, because exact
structure-recognition parity needs model definitions and template annotations,
not layout generation.
